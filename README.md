# MeowMood App 🐱
ระบบทำนายอารมณ์แมวจากภาพถ่ายด้วย Deep Learning

## 1. Project Description
MeowMood เป็นเว็บแอปพลิเคชันที่ใช้โมเดล Deep Learning (TensorFlow) สำหรับจำแนกอารมณ์แมวจากภาพถ่าย ได้แก่ Angry, Neutral, Relaxed และ Scared โดยผู้ใช้สามารถอัปโหลดรูปแมวผ่านหน้าเว็บที่พัฒนาโดย Streamlit

## 2. Project Structure
MeowMood-App/
│── app.py # ไฟล์หลักสำหรับรันเว็บแอป
│── meowmood_model.h5 # โมเดลที่เทรนแล้ว
│── meowmood_train.ipynb # Notebook สำหรับเทรนโมเดล
│── style.css # ไฟล์ตกแต่ง UI
│── requirements.txt # Dependencies
│── images/ # รูปภาพใช้ในหน้าเว็บ
│── README.md

## 3. Setup
1. ติดตั้ง Python 3.9 ขึ้นไป  
2. (แนะนำ) สร้าง Virtual Environment  

Windows:
python -m venv venv
venv\Scripts\activate

macOS / Linux:
python -m venv venv
source venv/bin/activate

ติดตั้ง dependencies
pip install -r requirements.txt

## 4. Run Application
แบบสั้น:
streamlit run app.py

แบบยาว (หากระบบหา streamlit ไม่เจอ):
python -m streamlit run app.py
เมื่อรันสำเร็จ ระบบจะเปิดเว็บผ่านเบราว์เซอร์ที่ URL เช่น
http://localhost:8501

5. Usage
เปิดเว็บตาม URL ที่ระบบแสดง
อัปโหลดภาพแมว (รองรับ JPG/PNG)
กดปุ่ม Analyze mood
ระบบจะทำนายอารมณ์แมวและแสดงความมั่นใจของโมเดล
